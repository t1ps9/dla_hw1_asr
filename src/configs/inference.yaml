defaults:
  - model: ds2
  - metrics: example
  - datasets: inference # we do not want to run inference on training data
  - dataloader: example
  - transforms: example_only_instance
  - _self_
inferencer:
  device_tensors: ["spectrogram", "text_encoded"] # which tensors should be on device (ex. GPU)
  device: auto # device name or "auto"
  save_path: "prediction_text" # any name here, can be a dataset name
  seed: 1
  from_pretrained: "to_download/model_best.pth" # path to the pretrained model
text_encoder:
  _target_: src.text_encoder.CTCTextEncoder
  vocab_path: to_download/librispeech-vocab.txt
  lm_pretrained_path: to_download/3-gram.pruned.1e-7.arpa

